============
Installation
============

At the command line::

    $ easy_install django-admin-model-reorder

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-admin-model-reorder
    $ pip install django-admin-model-reorder